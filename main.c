#include <stdio.h>
#include <stdbool.h>
#define boolean _Bool
int main() {
   void generate() {
  printf("Placed Block");
}
   bool Minecraft = true;
   if (boolean)( Minecraft = true); {
   generate();
   }
}
